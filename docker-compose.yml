services:
  # ===================
  # ftp(file trancefar protcol)
  # ===================
  ftp:
    container_name: ftp
    build: ./services/ftp
    restart: always
    env_file: .env
    networks:
      - inception_net
    volumes:
      - wordpress_data:/var/www/html
    ports:
      - "21:21"
      - "21000-21010:21000-21010"
  # ===================
  # MariaDB (Database)
  # ===================
  mariadb:
    container_name: mariadb
    build: ./services/marinadb
    restart: always
    env_file: .env
    networks:
      - inception_net
    volumes:
      - mariadb_data:/var/lib/mysql

  # ======================
  #   Redis Cache
  # ======================
  redis:
    container_name: redis
    build: ./services/redis
    restart: always
    
    networks:
      - inception_net

  # ======================
  #   WordPress + PHP-FPM
  # ======================
  wordpress:
    container_name: wordpress
    build: ./services/wordpress
    restart: always
    env_file: .env
    depends_on:
      - mariadb
      - redis
    networks:
      - inception_net
    volumes:
      - wordpress_data:/var/www/html

  # ======================
  #   NGINX
  # ======================
  nginx:
    container_name: nginx
    build: ./services/nginx
    restart: always
    env_file: .env
    depends_on:
      - wordpress
    ports:
      - "443:443"
    networks:
      - inception_net
    volumes:
      - wordpress_data:/var/www/html

# ======================
#   Networks
# ======================
networks:
  inception_net:
    driver: bridge
    name: inception_net

# ======================
#   Volumes
# ======================
volumes:
  mariadb_data:
    driver: local
    driver_opts:
      type: none
      device: ${DATA_PATH}/mariadb
      o: bind
  wordpress_data:
    driver: local
    driver_opts:
      type: none
      device: ${DATA_PATH}/wordpress
      o: bind
